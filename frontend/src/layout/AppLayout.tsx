import { Building2, CalendarDays, FileText, LayoutDashboard, LogOut, Menu, Receipt, Settings, WalletCards, X } from 'lucide-react'; import { NavLink, Outlet } from 'react-router-dom'; import { useAuth } from '@/context/AuthContext'; import { useState } from 'react'; import { cn } from '@/lib/utils'
const items=[['/dashboard','Dashboard',LayoutDashboard],['/contratantes','Contratantes',Building2],['/plantoes','Plantões',CalendarDays],['/financeiro','Financeiro',WalletCards],['/despesas','Despesas',Receipt],['/notas-fiscais','Notas Fiscais',FileText],['/impostos','Impostos',FileText],['/calendario','Calendário',CalendarDays],['/configuracoes','Configurações',Settings]] as const
export function AppLayout(){const {user,logout}=useAuth(),[open,setOpen]=useState(false);const side=<aside className="flex h-full w-64 flex-col border-r bg-white p-4"><div className="mb-8 px-2 text-xl font-bold text-slate-900"><span className="text-blue-600">Med</span>Money</div><nav className="space-y-1">{items.map(([path,label,Icon])=><NavLink onClick={()=>setOpen(false)} key={path} to={path} className={({isActive})=>cn('flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium text-slate-500 hover:bg-slate-50 hover:text-slate-900',isActive&&'bg-blue-50 text-blue-700')}><Icon size={18}/>{label}</NavLink>)}</nav><div className="mt-auto border-t pt-4"><NavLink to="/perfil" className="flex items-center gap-3 rounded-lg p-2 hover:bg-slate-50"><div className="grid h-8 w-8 place-items-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">{user?.name.slice(0,2).toUpperCase()}</div><span className="min-w-0 flex-1 truncate text-sm font-medium">{user?.name}</span></NavLink><button onClick={()=>void logout()} className="mt-2 flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-slate-500 hover:bg-slate-50"><LogOut size={18}/>Sair</button></div></aside>;return <div className="min-h-screen"><div className="hidden h-screen md:fixed md:inset-y-0 md:flex">{side}</div>{open&&<div className="fixed inset-0 z-40 bg-slate-900/30 md:hidden" onClick={()=>setOpen(false)}/>}<div className={cn('fixed inset-y-0 left-0 z-50 transition-transform md:hidden',open?'translate-x-0':'-translate-x-full')}>{side}<button className="absolute left-64 top-3 rounded bg-white p-2" onClick={()=>setOpen(false)}><X size={18}/></button></div><main className="md:pl-64"><header className="flex h-16 items-center border-b bg-white px-5"><button className="md:hidden" onClick={()=>setOpen(true)}><Menu/></button><div className="ml-auto text-sm text-slate-500">Olá, {user?.name.split(' ')[0]}</div></header><div className="p-5 sm:p-8"><Outlet/></div></main></div>}
