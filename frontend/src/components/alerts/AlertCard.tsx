import { motion } from 'framer-motion';import { BellRing } from 'lucide-react';import { Link } from 'react-router-dom';import type { AlertItem } from '@/types/alert';import { PriorityChip } from './PriorityChip';export function AlertCard({item}:{item:AlertItem}){return <motion.article initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} whileHover={{y:-2}} className={`card p-5 ${item.status==='Novo'?'border-blue-200':''}`}><div className="flex justify-between"><span className="rounded-xl bg-slate-50 p-2 text-blue-600"><BellRing size={18}/></span><PriorityChip priority={item.prioridade}/></div><p className="mt-4 text-xs font-medium uppercase text-slate-400">{item.categoria}</p><h3 className="mt-1 font-semibold">{item.titulo}</h3><p className="mt-2 line-clamp-2 text-sm text-slate-500">{item.descricao}</p><div className="mt-4 flex items-center justify-between border-t pt-3"><span className="text-xs text-slate-400">{new Date(item.updated_at).toLocaleDateString('pt-BR')}</span><Link className="text-sm font-medium text-blue-600" to={`/alertas/${item.id}`}>Resolver →</Link></div></motion.article>}
