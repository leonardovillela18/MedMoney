import { Link } from 'react-router-dom';import type { AlertItem } from '@/types/alert';import { PriorityChip } from './PriorityChip';export function AlertWidget({items}:{items:AlertItem[]}){return <section className="card p-5"><div className="flex justify-between"><h2 className="font-semibold">Alertas importantes</h2><Link className="text-sm text-blue-600" to="/alertas">Ver central</Link></div><div className="mt-4 space-y-3">{items.slice(0,5).map(x=><Link to={`/alertas/${x.id}`} className="block rounded-lg border p-3 hover:bg-slate-50" key={x.id}><div className="flex justify-between gap-3"><p className="text-sm font-medium">{x.titulo}</p><PriorityChip priority={x.prioridade}/></div><p className="mt-2 text-xs text-slate-500">{x.acao}</p></Link>)}{!items.length&&<p className="text-sm text-slate-400">Nenhum alerta importante ativo.</p>}</div></section>}
