import type { AlertItem } from '@/types/alert';import { AlertCard } from './AlertCard';export function AlertCenter({items}:{items:AlertItem[]}){return items.length?<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(x=><AlertCard item={x} key={x.id}/>)}</div>:<div className="card p-10 text-center text-sm text-slate-400">Nenhum alerta corresponde aos filtros.</div>}
