import type { AlertItem } from '@/types/alert';import { PriorityChip } from './PriorityChip';export function AlertTimeline({items}:{items:AlertItem[]}){return <div>{items.map(x=><article className="relative border-l-2 border-slate-200 pb-6 pl-6" key={x.id}><span className={`absolute -left-2 top-0 h-3.5 w-3.5 rounded-full border-2 border-white ${x.status==='Resolvido'?'bg-emerald-500':'bg-blue-600'}`}/><div className="flex items-center gap-2"><h3 className="font-semibold">{x.titulo}</h3><PriorityChip priority={x.prioridade}/></div><p className="mt-1 text-xs text-slate-400">{x.status} · {new Date(x.updated_at).toLocaleString('pt-BR')}</p></article>)}</div>}
