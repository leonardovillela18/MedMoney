import { useMemo } from 'react';import type { SeriesPoint } from '@/types/analytics';export function AnalyticsChart({title,question,data,type='bar'}:{title:string;question:string;data:SeriesPoint[];type?:'bar'|'line'|'area'|'pie'|'sparkline'}){const points=data.slice(-24),max=Math.max(1,...points.map(x=>Math.abs(x.value)));const path=useMemo(()=>points.map((x,i)=>`${i?'L':'M'} ${points.length===1?150:i/(points.length-1)*300} ${100-Math.abs(x.value)/max*85}`).join(' '),[points,max]);return <section className="card p-5"><h2 className="font-semibold">{title}</h2><p className="mt-1 text-xs text-slate-400">{question}</p>{type==='bar'?<div className="mt-5 flex h-40 items-end gap-2 overflow-x-auto border-b">{points.map(x=><div className="flex min-w-8 flex-1 flex-col items-center justify-end" key={x.label}><div title={`${x.label}: ${x.value}`} className="w-full max-w-8 rounded-t bg-blue-500" style={{height:`${Math.max(3,Math.abs(x.value)/max*125)}px`}}/><span className="mt-2 max-w-12 truncate text-[9px] text-slate-400">{x.label}</span></div>)}</div>:<svg className="mt-5 h-40 w-full" viewBox="0 0 300 110" role="img"><defs><linearGradient id="area" x1="0" y1="0" x2="0" y2="1"><stop stopColor="#2563eb" stopOpacity=".3"/><stop offset="1" stopColor="#2563eb" stopOpacity="0"/></linearGradient></defs>{type==='area'&&<path d={`${path} L 300 105 L 0 105 Z`} fill="url(#area)"/>}<path d={path} fill="none" stroke="#2563eb" strokeWidth="3" strokeLinecap="round"/></svg>} {!points.length&&<p className="py-12 text-center text-sm text-slate-400">Sem dados no período selecionado.</p>}</section>}
