import { Download } from 'lucide-react';import { Button } from '@/components/ui/Button';import { analyticsService } from '@/services/analytics';export function ReportCard(){return <section className="card p-6"><h2 className="font-semibold">Exportação executiva</h2><p className="mt-2 text-sm text-slate-500">Exporte indicadores e comparativos calculados no backend.</p><div className="mt-5 flex flex-wrap gap-2">{(['pdf','xlsx','csv','svg'] as const).map(x=><Button className="gap-2" onClick={()=>void analyticsService.export(x)} key={x}><Download size={16}/>{x.toUpperCase()}</Button>)}</div></section>}
