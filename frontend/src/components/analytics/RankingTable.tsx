import type { RankingPoint } from '@/types/analytics';export function RankingTable({title,items,format=(n)=>n.toLocaleString('pt-BR')}:{title:string;items:RankingPoint[];format?:(n:number)=>string}){const max=Math.max(1,...items.map(x=>x.value));return <section className="card overflow-hidden"><h2 className="p-5 font-semibold">{title}</h2><div>{items.slice(0,10).map((x,i)=><div className="grid grid-cols-[28px_1fr_auto] items-center gap-3 border-t p-3 text-sm" key={`${x.name}-${i}`}><b className="text-slate-300">{i+1}</b><div><p className="truncate font-medium">{x.name}</p><div className="mt-1 h-1 rounded bg-slate-100"><div className="h-full rounded bg-blue-500" style={{width:`${x.value/max*100}%`}}/></div></div><span>{format(x.value)}</span></div>)}</div></section>}
