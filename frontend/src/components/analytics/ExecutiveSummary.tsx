import { Clock,Coins,Percent,Stethoscope } from 'lucide-react';import { AnalyticsCard } from './AnalyticsCard';export function ExecutiveSummary({kpis}:{kpis:Record<string,number|string>}){const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><AnalyticsCard label="Receita bruta" value={money(Number(kpis.gross_revenue))} icon={Coins}/><AnalyticsCard label="Lucro" value={money(Number(kpis.profit))} icon={Coins}/><AnalyticsCard label="Margem" value={`${Number(kpis.margin).toFixed(1)}%`} icon={Percent}/><AnalyticsCard label="Plantões" value={String(kpis.shifts)} icon={Stethoscope}/><AnalyticsCard label="Horas trabalhadas" value={`${Number(kpis.hours).toFixed(1)}h`} icon={Clock}/><AnalyticsCard label="Média por hora" value={money(Number(kpis.average_hour))} icon={Clock}/><AnalyticsCard label="Média por plantão" value={money(Number(kpis.average_shift))} icon={Coins}/><AnalyticsCard label="Maior contratante" value={String(kpis.largest_contractor)} icon={Stethoscope}/></div>}
