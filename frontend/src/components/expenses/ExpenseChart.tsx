export function ExpenseChart({data}:{data:{name:string;value:number}[]}){const max=Math.max(1,...data.map(x=>x.value));return <section className="card p-5"><h2 className="font-semibold">Despesas por categoria</h2><div className="mt-5 space-y-3">{data.slice(0,8).map(x=><div key={x.name}><div className="mb-1 flex justify-between text-xs"><span>{x.name}</span><span>{x.value.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}</span></div><div className="h-2 rounded bg-slate-100"><div className="h-full rounded bg-blue-600" style={{width:`${x.value/max*100}%`}}/></div></div>)}</div></section>}
