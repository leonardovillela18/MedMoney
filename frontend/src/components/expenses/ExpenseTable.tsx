import { Link } from 'react-router-dom';import type { Category,Expense } from '@/types/expense';const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});export function ExpenseTable({items,categories,onDelete}:{items:Expense[];categories:Category[];onDelete:(id:string)=>void}){const names=Object.fromEntries(categories.map(x=>[x.id,x.nome]));return <div className="overflow-x-auto"><table className="w-full min-w-[850px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr>{['Data','Categoria','Fornecedor','Valor','Tipo','Status','Pagamento','Ações'].map(x=><th className="p-3" key={x}>{x}</th>)}</tr></thead><tbody>{items.map(x=><tr className="border-t" key={x.id}><td className="p-3">{new Date(`${x.competencia}T12:00`).toLocaleDateString('pt-BR')}</td><td className="p-3">{names[x.categoria_id]}</td><td className="p-3">{x.fornecedor||'—'}</td><td className="p-3 font-semibold">{money(x.valor)}</td><td className="p-3">{x.tipo}</td><td className="p-3"><span className="rounded-full bg-slate-100 px-2 py-1 text-xs">{x.status}</span></td><td className="p-3">{x.forma_pagamento}</td><td className="p-3"><div className="flex gap-3"><Link className="text-blue-600" to={`/despesas/${x.id}`}>Detalhes</Link><Link className="text-blue-600" to={`/despesas/${x.id}/editar`}>Editar</Link><button className="text-red-600" onClick={()=>onDelete(x.id)}>Excluir</button></div></td></tr>)}</tbody></table></div>}
