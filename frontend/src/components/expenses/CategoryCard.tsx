export function CategoryCard({name,value,total}:{name:string;value:number;total:number}){const pct=total?value/total*100:0;return <article className="rounded-xl border bg-white p-4"><div className="flex justify-between text-sm"><b>{name}</b><span>{pct.toFixed(0)}%</span></div><div className="mt-3 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600" style={{width:`${pct}%`}}/></div></article>}
