import type { Expense } from '@/types/expense';export function ExpenseTimeline({items}:{items:Expense[]}){return <section className="card p-5"><h2 className="font-semibold">Próximos vencimentos</h2><div className="mt-4 space-y-3">{items.slice(0,6).map(x=><div className="flex justify-between border-l-2 border-amber-400 pl-3 text-sm" key={x.id}><span>{new Date(`${x.data_vencimento}T12:00`).toLocaleDateString('pt-BR')} · {x.titulo}</span><b>{x.valor.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}</b></div>)}</div></section>}
