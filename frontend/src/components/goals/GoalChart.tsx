import type { GoalDetail } from '@/types/goal';export function GoalChart({history}:{history:GoalDetail['history']}){const max=Math.max(1,...history.map(x=>x.value)),path=history.map((x,i)=>`${i?'L':'M'} ${history.length===1?150:i/(history.length-1)*300} ${100-x.value/max*85}`).join(' ');return <section className="card p-5"><h2 className="font-semibold">Evolução da meta</h2><p className="mt-1 text-xs text-slate-400">Snapshots diários calculados automaticamente.</p>{history.length?<svg className="mt-5 h-48 w-full" viewBox="0 0 300 110"><path d={path} fill="none" stroke="#2563eb" strokeWidth="3" strokeLinecap="round"/></svg>:<p className="py-16 text-center text-sm text-slate-400">O histórico começará após as próximas atualizações.</p>}</section>}
