export function GoalProgress({percentage,color='blue'}:{percentage:number;color?:string}){const colors:Record<string,string>={blue:'bg-blue-600',green:'bg-emerald-500',violet:'bg-violet-500',amber:'bg-amber-500',red:'bg-red-500'};return <div><div className="flex justify-between text-xs"><span>Progresso</span><b>{percentage.toFixed(1)}%</b></div><div className="mt-2 h-2 rounded-full bg-slate-100"><div className={`h-full rounded-full transition-all ${colors[color]??colors.blue}`} style={{width:`${Math.min(percentage,100)}%`}}/></div></div>}
