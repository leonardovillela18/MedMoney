import { motion } from 'framer-motion';import { Target } from 'lucide-react';import { Link } from 'react-router-dom';import type { Goal } from '@/types/goal';import { GoalProgress } from './GoalProgress';const format=(g:Goal,n:number)=>['Quantidade de Plantões'].includes(g.tipo)?n.toLocaleString('pt-BR'):g.tipo==='Horas Trabalhadas'?`${n.toFixed(1)}h`:n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});export function GoalCard({goal}:{goal:Goal}){return <motion.article whileHover={{y:-2}} className="card p-5"><div className="flex justify-between"><span className="rounded-xl bg-blue-50 p-2 text-blue-600"><Target size={19}/></span><span className="rounded-full bg-slate-100 px-2 py-1 text-xs">{goal.status}</span></div><p className="mt-4 text-xs uppercase text-slate-400">{goal.tipo}</p><h3 className="mt-1 font-semibold">{goal.titulo}</h3><p className="mt-3 text-sm text-slate-500">{format(goal,goal.valor_atual)} de {format(goal,goal.valor_meta)}</p><div className="mt-4"><GoalProgress percentage={goal.percentual} color={goal.cor}/></div><Link className="mt-4 inline-block text-sm font-medium text-blue-600" to={`/metas/${goal.id}`}>Ver acompanhamento →</Link></motion.article>}
