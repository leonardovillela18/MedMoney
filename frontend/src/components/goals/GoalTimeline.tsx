import type { GoalDetail } from '@/types/goal';export function GoalTimeline({items}:{items:GoalDetail['history']}){return <section className="card p-5"><h2 className="font-semibold">Histórico</h2><div className="mt-5">{[...items].reverse().map(x=><div className="border-l-2 border-blue-100 pb-5 pl-5" key={x.date}><p className="text-sm font-medium">{x.percentage.toFixed(1)}% concluído</p><p className="text-xs text-slate-400">{new Date(`${x.date}T12:00`).toLocaleDateString('pt-BR')} · valor {x.value.toLocaleString('pt-BR')}</p></div>)}</div></section>}
