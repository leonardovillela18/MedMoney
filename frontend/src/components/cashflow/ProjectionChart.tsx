import type { CashflowProjection } from '@/types/cashflow';export function ProjectionChart({series}:{series:CashflowProjection['series']}){const points=series.slice(-60),max=Math.max(1,...points.flatMap(x=>[Math.abs(x.inflow),Math.abs(x.outflow),Math.abs(x.balance)]));return <section className="card p-5"><h2 className="font-semibold">Entradas x Saídas e saldo acumulado</h2><p className="mt-1 text-sm text-slate-500">Fluxo diário derivado das movimentações existentes.</p><div className="mt-6 flex h-52 items-end gap-1 overflow-x-auto border-b">{points.length?points.map(x=><div key={x.date} className="flex min-w-3 flex-1 items-end gap-px" title={`${x.date} · saldo ${x.balance}`}><span className="w-1/3 bg-emerald-400" style={{height:`${Math.max(2,x.inflow/max*180)}px`}}/><span className="w-1/3 bg-red-300" style={{height:`${Math.max(2,x.outflow/max*180)}px`}}/><span className={`w-1/3 ${x.balance<0?'bg-red-600':'bg-blue-600'}`} style={{height:`${Math.max(2,Math.abs(x.balance)/max*180)}px`}}/></div>):<p className="m-auto text-sm text-slate-400">Nenhum dado para projetar.</p>}</div><div className="mt-4 flex gap-4 text-xs text-slate-500"><span className="text-emerald-600">● Entradas</span><span className="text-red-500">● Saídas</span><span className="text-blue-600">● Saldo</span></div></section>}
