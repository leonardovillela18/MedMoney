import { Component,type ErrorInfo,type ReactNode } from 'react';import { Button } from '@/components/ui/Button';export class AppErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}componentDidCatch(error:Error,info:ErrorInfo){if(import.meta.env.DEV)console.error(error,info)}render(){if(this.state.failed)return <main className="grid min-h-screen place-items-center p-6"><section className="card max-w-lg p-8 text-center" role="alert"><h1 className="text-xl font-bold">Não foi possível exibir esta tela.</h1><p className="mt-2 text-sm text-slate-500">Seus dados estão seguros. Recarregue para tentar novamente.</p><Button className="mt-5" onClick={()=>window.location.reload()}>Recarregar aplicação</Button></section></main>;return this.props.children}}
