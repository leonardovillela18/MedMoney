import { motion } from 'framer-motion';import { CircleDollarSign } from 'lucide-react';import { Link } from 'react-router-dom';import type { Insight } from '@/types/insight';import { InsightBadge } from './InsightBadge';export function InsightCard({item}:{item:Insight}){return <motion.article whileHover={{y:-2}} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} className="card p-5"><div className="flex items-start justify-between gap-3"><span className="rounded-xl bg-slate-50 p-2 text-blue-600"><CircleDollarSign size={19}/></span><InsightBadge severity={item.severidade}/></div><p className="mt-4 text-xs font-medium uppercase tracking-wide text-slate-400">{item.categoria}</p><h3 className="mt-1 font-semibold leading-snug">{item.titulo}</h3><p className="mt-2 line-clamp-2 text-sm leading-relaxed text-slate-500">{item.descricao}</p><div className="mt-4 flex items-center justify-between border-t pt-3"><span className="text-xs text-slate-400">Prioridade {item.prioridade}</span><Link className="text-sm font-medium text-blue-600" to={`/insights/${item.id}`}>Entender →</Link></div></motion.article>}
