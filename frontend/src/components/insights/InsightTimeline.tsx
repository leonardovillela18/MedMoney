import type { Insight } from '@/types/insight';import { InsightBadge } from './InsightBadge';export function InsightTimeline({items}:{items:Insight[]}){return <div>{items.map(x=><article className="relative border-l-2 border-slate-200 pb-6 pl-6" key={x.id}><span className="absolute -left-2 top-0 h-3.5 w-3.5 rounded-full border-2 border-white bg-blue-600"/><div className="flex flex-wrap items-center gap-2"><h3 className="font-semibold">{x.titulo}</h3><InsightBadge severity={x.severidade}/></div><p className="mt-1 text-sm text-slate-500">{new Date(x.updated_at).toLocaleDateString('pt-BR')} · {x.categoria}</p><p className="mt-2 text-sm">{x.acao_recomendada}</p></article>)}</div>}
