import type { Insight } from '@/types/insight';import { InsightCard } from './InsightCard';export function InsightList({items}:{items:Insight[]}){return items.length?<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(x=><InsightCard item={x} key={x.id}/>)}</div>:<div className="card p-10 text-center text-sm text-slate-400">Nenhum insight corresponde aos filtros.</div>}
