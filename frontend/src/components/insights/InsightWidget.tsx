import { Link } from 'react-router-dom';import type { Insight } from '@/types/insight';import { InsightBadge } from './InsightBadge';export function InsightWidget({items}:{items:Insight[]}){return <section className="card p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">Insights do Dia</h2><Link className="text-sm text-blue-600" to="/insights">Ver todos</Link></div><div className="mt-4 space-y-3">{items.slice(0,3).map(x=><Link to={`/insights/${x.id}`} key={x.id} className="block rounded-lg border p-3 transition hover:bg-slate-50"><div className="flex items-start justify-between gap-3"><p className="text-sm font-medium">{x.titulo}</p><InsightBadge severity={x.severidade}/></div><p className="mt-2 text-xs text-slate-500">{x.acao_recomendada}</p></Link>)}</div></section>}
