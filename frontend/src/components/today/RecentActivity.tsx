import type { TodayData } from '@/types/today';export function RecentActivity({items}:{items:TodayData['activity']}){return <section className="card p-5"><h2 className="font-semibold">Atividade recente</h2><div className="mt-4 space-y-3">{items.map(x=><div className="border-l-2 border-slate-200 pl-3" key={`${x.type}-${x.at}`}><p className="text-sm font-medium">{x.type}</p><p className="text-xs text-slate-400">{x.description} · {new Date(x.at).toLocaleDateString('pt-BR')}</p></div>)}</div></section>}
