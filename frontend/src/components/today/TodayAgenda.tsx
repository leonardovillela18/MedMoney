import type { TodayData } from '@/types/today';const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});export function TodayAgenda({items}:{items:TodayData['agenda']}){return <section className="card p-5"><h2 className="font-semibold">Próximos plantões</h2><div className="mt-4 space-y-3">{items.length?items.map(x=><article className="flex items-center justify-between gap-4 border-b pb-3 last:border-0" key={x.id}><div><p className="text-sm font-medium">{x.hospital}</p><p className="mt-1 text-xs text-slate-400">{new Date(`${x.date}T12:00`).toLocaleDateString('pt-BR')} às {x.time} · {x.specialty||'Especialidade não informada'}</p></div><span className="text-sm font-semibold">{money(x.value)}</span></article>):<p className="text-sm text-slate-400">Nenhum plantão futuro agendado.</p>}</div></section>}
