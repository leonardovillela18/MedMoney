import { motion } from 'framer-motion';export function TodayHeader({name,date,message}:{name:string;date:string;message:string}){const hour=new Date().getHours(),greeting=hour<12?'Bom dia':hour<18?'Boa tarde':'Boa noite';return <motion.header initial={{opacity:0,y:-8}} animate={{opacity:1,y:0}} className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm capitalize text-slate-500">{new Date(`${date}T12:00`).toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'})}</p><h1 className="mt-1 text-3xl font-bold tracking-tight">{greeting}, Dr. {name.split(' ')[0]} <span aria-hidden>👋</span></h1><p className="mt-2 text-sm text-slate-500">{message}</p></div><span className="rounded-full border bg-white px-3 py-1.5 text-xs text-slate-500">Atualizado agora</span></motion.header>}
