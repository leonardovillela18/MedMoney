import { motion } from 'framer-motion';const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});export function TodaySummary({receivable,tax,net}:{receivable:number;tax:number;net:number}){return <motion.section initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:.05}} className="overflow-hidden rounded-2xl bg-slate-950 p-7 text-white shadow-xl"><p className="text-sm text-slate-400">Você possui</p><p className="mt-1 text-4xl font-bold tracking-tight">{money(receivable)}</p><p className="text-sm text-slate-400">para receber</p><div className="mt-7 grid gap-5 border-t border-slate-800 pt-5 sm:grid-cols-2"><div><p className="text-xs text-slate-400">Reserva tributária sugerida</p><p className="mt-1 text-xl font-semibold text-amber-300">{money(tax)}</p></div><div><p className="text-xs text-slate-400">Lucro líquido estimado</p><p className="mt-1 text-xl font-semibold text-emerald-400">{money(net)}</p></div></div></motion.section>}
