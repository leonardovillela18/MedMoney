import { motion } from 'framer-motion';const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});export function TodaySummary({receivable,current,tax,net}:{receivable:number;current:number;tax:number;net:number}){return <motion.section initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:.05}} className="overflow-hidden rounded-2xl bg-slate-950 p-7 text-white shadow-xl"><div className="grid gap-5 sm:grid-cols-2"><div><p className="text-sm text-slate-400">A receber</p><p className="mt-1 break-words text-3xl font-bold tracking-tight sm:text-4xl">{money(receivable)}</p><p className="text-xs text-slate-400">Entradas previstas; ainda não compõem o saldo</p></div><div className="rounded-xl bg-white/5 p-4"><p className="text-sm text-slate-400">Saldo atual</p><p className="mt-1 break-words text-2xl font-bold text-emerald-400">{money(current)}</p><p className="text-xs text-slate-400">Somente pagamentos confirmados</p></div></div><div className="mt-7 grid gap-5 border-t border-slate-800 pt-5 sm:grid-cols-2"><div><p className="text-xs text-slate-400">Reserva tributária sugerida</p><p className="mt-1 text-xl font-semibold text-amber-300">{money(tax)}</p></div><div><p className="text-xs text-slate-400">Lucro líquido estimado</p><p className="mt-1 text-xl font-semibold text-emerald-400">{money(net)}</p></div></div></motion.section>}
