import type { TodayData } from '@/types/today';const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});export function TodayPayments({items}:{items:TodayData['payments']}){return <section className="card p-5"><h2 className="font-semibold">Próximos recebimentos</h2><div className="mt-4 space-y-3">{items.length?items.map(x=><article className="flex justify-between gap-4 border-b pb-3 last:border-0" key={x.id}><div><p className="text-sm font-medium">{x.contractor}</p><p className={`mt-1 text-xs ${x.days<0?'text-red-500':'text-slate-400'}`}>{x.days<0?`${Math.abs(x.days)} dias em atraso`:x.days===0?'Previsto para hoje':`Em ${x.days} dias`}</p></div><b className="text-sm">{money(x.value)}</b></article>):<p className="text-sm text-slate-400">Nenhum recebimento pendente.</p>}</div></section>}
