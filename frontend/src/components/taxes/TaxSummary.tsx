export function TaxSummary({coverage,disclaimer}:{coverage:number;disclaimer:string}){return <section className="card border-blue-100 bg-gradient-to-r from-blue-50 to-white p-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm font-semibold text-blue-900">Cobertura da reserva sugerida</p><p className="mt-1 text-sm text-slate-600">{coverage.toFixed(0)}% das estimativas acumuladas já estão reservadas.</p></div><strong className="text-2xl text-blue-700">{coverage.toFixed(0)}%</strong></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-blue-100"><div className="h-full rounded-full bg-blue-600 transition-all" style={{width:`${Math.min(coverage,100)}%`}}/></div><p className="mt-4 text-xs text-slate-500">{disclaimer}</p></section>}
