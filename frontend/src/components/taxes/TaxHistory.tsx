import type { TaxEstimation } from '@/types/tax';import { ReserveCard } from './ReserveCard';const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})
export function TaxHistory({items,onReserved}:{items:TaxEstimation[];onReserved:()=>void}){return <section className="card overflow-hidden"><div className="border-b p-5"><h2 className="font-semibold">Histórico de estimativas</h2><p className="mt-1 text-sm text-slate-500">Valores projetados por competência.</p></div><div className="divide-y">{items.length?items.map(x=><article key={x.id} className="grid gap-4 p-5 md:grid-cols-[1fr_1fr_auto] md:items-center"><div><p className="font-medium">{new Date(`${x.competencia}T12:00`).toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</p><p className="mt-1 text-sm text-slate-500">Receita {money(x.base_calculo)} · {x.percentual}% · {x.tipo}</p></div><div><p className="text-xs uppercase tracking-wide text-slate-400">Valor estimado</p><p className="font-semibold">{money(x.valor_estimado)}</p><span className="mt-1 inline-block rounded-full bg-slate-100 px-2 py-1 text-xs">{x.status}</span></div><ReserveCard item={x} onSuccess={onReserved}/></article>):<p className="p-8 text-center text-sm text-slate-400">Nenhuma estimativa encontrada.</p>}</div></section>}
