import { api } from '@/services/api';import type { AlertDashboard,AlertItem,AlertPageData } from '@/types/alert';export const alertsService={list:(params:Record<string,string|number|undefined>={})=>api.get<AlertPageData>('/alerts',{params}).then(r=>r.data),get:(id:string)=>api.get<AlertItem>(`/alerts/${id}`).then(r=>r.data),dashboard:()=>api.get<AlertDashboard>('/alerts/dashboard').then(r=>r.data),read:(id:string)=>api.patch<AlertItem>(`/alerts/${id}/read`).then(r=>r.data),resolve:(id:string)=>api.patch<AlertItem>(`/alerts/${id}/resolve`).then(r=>r.data)}
