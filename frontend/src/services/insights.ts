import { api } from '@/services/api';import type { Insight,InsightDashboard,InsightPageData } from '@/types/insight';export const insightsService={list:(params:Record<string,string|number|undefined>={})=>api.get<InsightPageData>('/insights',{params}).then(r=>r.data),get:(id:string)=>api.get<Insight>(`/insights/${id}`).then(r=>r.data),dashboard:()=>api.get<InsightDashboard>('/insights/dashboard').then(r=>r.data),recalculate:()=>api.post<{updated:number}>('/insights/recalculate').then(r=>r.data)}
