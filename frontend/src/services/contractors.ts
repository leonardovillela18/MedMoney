import { api } from './api'; import type { Contractor, ContractorPage } from '@/types/contractor'
export const contractorsService={list:(params:Record<string,string|number|boolean|undefined>)=>api.get<ContractorPage>('/contractors',{params}).then(r=>r.data),get:(id:string)=>api.get<Contractor>(`/contractors/${id}`).then(r=>r.data),create:(data:Omit<Contractor,'id'>)=>api.post<Contractor>('/contractors',data).then(r=>r.data),update:(id:string,data:Omit<Contractor,'id'>)=>api.put<Contractor>(`/contractors/${id}`,data).then(r=>r.data),remove:(id:string)=>api.delete(`/contractors/${id}`)}
