import { api } from './api'; import type { AuthTokens, User } from '@/types/auth'
export const authService = { login:(email:string,password:string)=>api.post<AuthTokens>('/auth/login',{email,password}).then(r=>r.data), register:(data:Record<string,string>)=>api.post<AuthTokens>('/auth/register',data).then(r=>r.data), me:()=>api.get<User>('/auth/me').then(r=>r.data), forgot:(email:string)=>api.post('/auth/forgot-password',{email}), reset:(token:string,password:string)=>api.post('/auth/reset-password',{token,password}), logout:(refresh_token:string)=>api.post('/auth/logout',{refresh_token}) }
