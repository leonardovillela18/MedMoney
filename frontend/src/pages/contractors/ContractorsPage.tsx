import { useQuery, useQueryClient } from '@tanstack/react-query'; import { Link } from 'react-router-dom'; import { Building2, Eye, Pencil, Plus, Search, Trash2 } from 'lucide-react'; import { useState } from 'react'; import { contractorsService } from '@/services/contractors'; import { Button } from '@/components/ui/Button'
export function ContractorsPage(){const [search,setSearch]=useState(''),[page,setPage]=useState(1),[confirm,setConfirm]=useState<string|null>(null),queryClient=useQueryClient();const {data,isLoading}=useQuery({queryKey:['contractors',search,page],queryFn:()=>contractorsService.list({search,page,page_size:10})});const remove=async()=>{if(!confirm)return;await contractorsService.remove(confirm);setConfirm(null);await queryClient.invalidateQueries({queryKey:['contractors']})};return <div><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm text-slate-500">Cadastros</p><h1 className="mt-1 text-2xl font-bold text-slate-900">Contratantes</h1></div><Link to="/contratantes/novo"><Button><Plus size={17} className="mr-2"/>Novo contratante</Button></Link></div><div className="card overflow-hidden"><div className="border-b p-4"><div className="relative max-w-sm"><Search className="absolute left-3 top-3 text-slate-400" size={17}/><input className="field mt-0 pl-9" placeholder="Pesquisar por nome" value={search} onChange={e=>{setSearch(e.target.value);setPage(1)}}/></div></div>{isLoading?<div className="space-y-3 p-5">{[1,2,3].map(i=><div key={i} className="h-12 animate-pulse rounded bg-slate-100"/>)}</div>:!data?.total?<div className="grid min-h-72 place-items-center p-6 text-center"><div><span className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-blue-50 text-blue-600"><Building2/></span><p className="mt-4 font-semibold">Você ainda não possui nenhum contratante cadastrado.</p><Link to="/contratantes/novo"><Button className="mt-5">Cadastrar primeiro contratante</Button></Link></div></div>:<><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{['Nome','Tipo','Cidade','Telefone','Prazo','Status','Ações'].map(x=><th key={x} className="px-5 py-3 font-medium">{x}</th>)}</tr></thead><tbody>{data.items.map(item=><tr key={item.id} className="border-t"><td className="px-5 py-4 font-medium"><Link className="hover:text-blue-600" to={`/contratantes/${item.id}`}>{item.name}</Link></td><td className="px-5 py-4 text-slate-600">{item.type}</td><td className="px-5 py-4 text-slate-600">{item.city||'—'}</td><td className="px-5 py-4 text-slate-600">{item.phone||item.mobile||'—'}</td><td className="px-5 py-4 text-slate-600">{item.payment_term_days?`${item.payment_term_days} dias`:'—'}</td><td className="px-5 py-4"><span className={item.active?'rounded-full bg-emerald-50 px-2 py-1 text-xs font-medium text-emerald-700':'rounded-full bg-slate-100 px-2 py-1 text-xs font-medium text-slate-600'}>{item.active?'Ativo':'Inativo'}</span></td><td className="px-5 py-4"><div className="flex gap-2 text-slate-500"><Link to={`/contratantes/${item.id}`} title="Visualizar"><Eye size={16}/></Link><Link to={`/contratantes/${item.id}/editar`} title="Editar"><Pencil size={16}/></Link><button title="Excluir" onClick={()=>setConfirm(item.id)}><Trash2 size={16}/></button></div></td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t p-4 text-sm text-slate-500"><span>{data.total} contratante(s)</span><div className="flex gap-2"><button disabled={page===1} onClick={()=>setPage(page-1)}>Anterior</button><button disabled={page*10>=data.total} onClick={()=>setPage(page+1)}>Próxima</button></div></div></> }</div>{confirm&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-900/30 p-5"><div className="card w-full max-w-sm p-6"><h2 className="font-bold">Excluir contratante?</h2><p className="mt-2 text-sm text-slate-500">Este cadastro deixará de aparecer nas listagens.</p><div className="mt-6 flex justify-end gap-3"><button onClick={()=>setConfirm(null)} className="text-sm">Cancelar</button><Button onClick={()=>void remove()} className="bg-red-600 hover:bg-red-700">Excluir</Button></div></div></div>}</div>}
