import { useState } from 'react'
import { BookOpen, Building2, Check, MapPin, Palette, Save, Stethoscope, UserRound } from 'lucide-react'
import { useAuth } from '@/context/AuthContext'
import { Button } from '@/components/ui/Button'
import { cn } from '@/lib/utils'
import { loadPreferences, savePreferences, type Preferences } from '@/lib/preferences'

type Tab='account'|'personalization';type ProfileExtras={courses:string;workplaces:string};const profileKey='medfinance_profile_extras'
function loadExtras():ProfileExtras{try{return{courses:'',workplaces:'',...JSON.parse(localStorage.getItem(profileKey)??'{}')}}catch{return{courses:'',workplaces:''}}}
export function SettingsPage(){const{user}=useAuth(),[tab,setTab]=useState<Tab>('account'),[extras,setExtras]=useState(loadExtras),[preferences,setPreferences]=useState<Preferences>(loadPreferences),[saved,setSaved]=useState(false);const confirmSaved=()=>{setSaved(true);window.setTimeout(()=>setSaved(false),2200)},saveProfile=()=>{localStorage.setItem(profileKey,JSON.stringify(extras));confirmSaved()},updatePreferences=(next:Preferences)=>{setPreferences(next);savePreferences(next);confirmSaved()};return <div className="mx-auto max-w-5xl"><div className="mb-8"><p className="text-sm text-slate-500">Preferências e perfil</p><h1 className="mt-1 text-2xl font-bold text-slate-900">Configurações</h1></div><div className="mb-6 flex w-full gap-1 overflow-x-auto sm:w-fit rounded-xl border bg-[var(--surface)] p-1 shadow-sm"><button onClick={()=>setTab('account')} className={cn('flex items-center gap-2 rounded-lg px-4 py-2 text-sm font-medium',tab==='account'?'bg-blue-600 text-white shadow-sm':'text-slate-500 hover:bg-slate-100')}><UserRound size={17}/>Conta</button><button onClick={()=>setTab('personalization')} className={cn('flex items-center gap-2 rounded-lg px-4 py-2 text-sm font-medium',tab==='personalization'?'bg-blue-600 text-white shadow-sm':'text-slate-500 hover:bg-slate-100')}><Palette size={17}/>Personalização</button></div>{saved&&<div role="status" className="fixed inset-x-3 top-20 sm:left-auto sm:right-5 z-40 flex items-center gap-2 rounded-lg bg-emerald-600 px-4 py-3 text-sm font-medium text-white shadow-lg"><Check size={17}/>Alterações salvas</div>}{tab==='account'?<div className="space-y-6"><section className="card p-6 sm:p-8"><div className="flex flex-col gap-5 border-b pb-6 sm:flex-row sm:items-center"><div className="grid h-20 w-20 place-items-center rounded-2xl bg-blue-100 text-2xl font-bold text-blue-700">{user?.name.slice(0,2).toUpperCase()}</div><div><p className="text-xl font-bold text-slate-900">{user?.name}</p><p className="mt-1 text-sm text-slate-500">{user?.specialty}</p><p className="mt-1 text-sm text-slate-400">{user?.email}</p></div></div><div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3"><Info icon={UserRound} label="Nome completo" value={user?.name}/><Info icon={Stethoscope} label="CRM" value={`${user?.crm} / ${user?.crm_uf}`}/><Info icon={BookOpen} label="Especialidade" value={user?.specialty}/><Info icon={MapPin} label="Cidade" value={`${user?.city} / ${user?.state}`}/><Info icon={UserRound} label="E-mail" value={user?.email}/></div></section><section className="card p-6 sm:p-8"><div className="mb-6"><h2 className="text-lg font-semibold text-slate-900">Informações profissionais</h2><p className="mt-1 text-sm text-slate-500">Complete os dados que ajudam a representar sua atuação profissional.</p></div><div className="space-y-5"><label className="label block"><span className="flex items-center gap-2"><BookOpen size={17}/>Cursos e certificações</span><textarea className="field min-h-28 resize-y" placeholder="Ex.: ACLS, Pós-graduação em Cardiologia, Gestão de clínicas..." value={extras.courses} onChange={e=>setExtras({...extras,courses:e.target.value})}/></label><label className="label block"><span className="flex items-center gap-2"><Building2 size={17}/>Locais onde atende</span><textarea className="field min-h-28 resize-y" placeholder="Ex.: Hospital Central, Clínica Vida, atendimento particular..." value={extras.workplaces} onChange={e=>setExtras({...extras,workplaces:e.target.value})}/></label></div><Button className="mt-6 gap-2" onClick={saveProfile}><Save size={17}/>Salvar informações</Button></section></div>:<section className="card p-6 sm:p-8"><div className="mb-7"><h2 className="text-lg font-semibold text-slate-900">Aparência do sistema</h2><p className="mt-1 text-sm text-slate-500">Escolha uma visualização confortável para sua rotina.</p></div><div className="space-y-8"><Preference title="Tema do sistema" description="Alterne entre o tema claro confortável e o tema escuro em azul-marinho."><Choice active={preferences.theme==='light'} title="Tema claro" detail="Cinza suave e cartões menos brilhantes" onClick={()=>updatePreferences({...preferences,theme:'light'})}/><Choice active={preferences.theme==='dark'} title="Tema escuro" detail="Azul-marinho com destaques em ciano" onClick={()=>updatePreferences({...preferences,theme:'dark'})}/></Preference><Preference title="Tamanho do texto" description="Aumente levemente os textos de todo o sistema."><Choice active={preferences.fontSize==='normal'} title="Normal" detail="Tamanho padrão" onClick={()=>updatePreferences({...preferences,fontSize:'normal'})}/><Choice active={preferences.fontSize==='large'} title="Maior" detail="Mais confortável para leitura" onClick={()=>updatePreferences({...preferences,fontSize:'large'})}/></Preference><div className="flex flex-col justify-between gap-4 border-t pt-6 sm:flex-row sm:items-center"><div><h3 className="font-medium text-slate-800">Reduzir animações</h3><p className="mt-1 text-sm text-slate-500">Minimiza transições e movimentos da interface.</p></div><button role="switch" aria-checked={preferences.reduceMotion} onClick={()=>updatePreferences({...preferences,reduceMotion:!preferences.reduceMotion})} className={cn('relative h-7 w-12 rounded-full transition',preferences.reduceMotion?'bg-blue-600':'bg-slate-300')}><span className={cn('absolute left-1 top-1 h-5 w-5 rounded-full bg-white shadow transition-transform',preferences.reduceMotion&&'translate-x-5')}/></button></div></div></section>}</div>}
function Info({icon:Icon,label,value}:{icon:typeof UserRound;label:string;value?:string}){return <div className="rounded-xl border bg-slate-50/70 p-4"><Icon size={18} className="mb-3 text-blue-600"/><p className="text-xs font-medium uppercase tracking-wide text-slate-400">{label}</p><p className="mt-1 break-words text-sm font-medium text-slate-700">{value||'Não informado'}</p></div>}
function Preference({title,description,children}:{title:string;description:string;children:React.ReactNode}){return <div><h3 className="font-medium text-slate-800">{title}</h3><p className="mt-1 text-sm text-slate-500">{description}</p><div className="mt-4 grid gap-3 sm:grid-cols-2">{children}</div></div>}
function Choice({active,title,detail,onClick}:{active:boolean;title:string;detail:string;onClick:()=>void}){return <button onClick={onClick} className={cn('rounded-xl border p-4 text-left transition',active?'border-blue-500 bg-blue-50 ring-1 ring-blue-500':'bg-[var(--surface)] hover:bg-slate-50')}><span className="flex items-center justify-between font-medium text-slate-800">{title}{active&&<Check size={17} className="text-blue-600"/>}</span><span className="mt-1 block text-xs text-slate-500">{detail}</span></button>}
