import { Link, useNavigate } from 'react-router-dom'; import { useForm } from 'react-hook-form'; import { z } from 'zod'; import { zodResolver } from '@hookform/resolvers/zod'; import { AuthShell } from './AuthShell'; import { Button } from '@/components/ui/Button'; import { useAuth } from '@/context/AuthContext'; import { useState } from 'react'
const schema=z.object({email:z.string().email('Informe um e-mail válido'),password:z.string().min(1,'Informe sua senha'),keep:z.boolean()});type Data=z.infer<typeof schema>
export function LoginPage(){const {login}=useAuth(),nav=useNavigate(),[error,setError]=useState('');const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm<Data>({resolver:zodResolver(schema),defaultValues:{keep:true}});const submit=async(d:Data)=>{try{setError('');await login(d.email,d.password,d.keep);nav('/dashboard')}catch{setError('Não conseguimos realizar o login agora. Verifique seus dados e tente novamente.')}};return <AuthShell title="Boas-vindas" subtitle="Entre para acompanhar sua vida financeira."><form onSubmit={handleSubmit(submit)} className="mt-8 space-y-5"><div><label className="label">E-mail</label><input className="field" autoComplete="email" placeholder="voce@email.com" {...register('email')}/>{errors.email&&<p className="mt-1 text-xs text-red-600">{errors.email.message}</p>}</div><div><div className="flex justify-between"><label className="label">Senha</label><Link className="text-xs font-medium text-blue-600" to="/esqueci-senha">Esqueci minha senha</Link></div><input className="field" type="password" autoComplete="current-password" {...register('password')}/>{errors.password&&<p className="mt-1 text-xs text-red-600">{errors.password.message}</p>}</div><label className="flex items-center gap-2 text-sm text-slate-600"><input type="checkbox" className="rounded border-slate-300 text-blue-600" {...register('keep')}/>Manter conectado</label>{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button className="w-full" disabled={isSubmitting}>{isSubmitting?'Entrando...':'Entrar'}</Button></form><p className="mt-7 text-center text-sm text-slate-500">Ainda não tem conta? <Link className="font-semibold text-blue-600" to="/cadastro">Criar conta</Link></p></AuthShell>}
