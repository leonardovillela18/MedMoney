import { useQuery } from '@tanstack/react-query'
import { useNavigate, useParams } from 'react-router-dom'
import { useEffect, useState } from 'react'
import { contractorsService } from '@/services/contractors'
import { shiftsService } from '@/services/shifts'
import { Button } from '@/components/ui/Button'
import type { Shift } from '@/types/shift'
export function SurgeryFormPage(){const{id}=useParams(),nav=useNavigate(),[form,setForm]=useState<Partial<Shift>>({type:'Cirurgia',status:'Agendado',gross_value:1,payment_method:'PIX'}),[error,setError]=useState('');const{data:contractors}=useQuery({queryKey:['contractors-surgery'],queryFn:()=>contractorsService.list({page:1,page_size:100})});const existing=useQuery({queryKey:['surgery',id],enabled:!!id,queryFn:()=>shiftsService.get(id!)});useEffect(()=>{if(existing.data)setForm(existing.data)},[existing.data]);const set=(key:keyof Shift,value:unknown)=>setForm({...form,[key]:value});const save=async(e:React.FormEvent)=>{e.preventDefault();try{setError('');await shiftsService.save({...form,type:'Cirurgia'} as Omit<Shift,'id'|'duration_hours'|'estimated_net_value'>,id);nav('/cirurgias')}catch{setError('Não foi possível salvar. Confira hospital, data, horário e valor.')}};return <form onSubmit={save} className="mx-auto max-w-3xl"><div className="mb-8 flex flex-col items-start gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm text-slate-500">Agenda cirúrgica</p><h1 className="text-2xl font-bold">{id?'Editar cirurgia':'Nova cirurgia'}</h1></div><Button>Salvar</Button></div><section className="card grid gap-5 p-4 min-[380px]:p-5 sm:grid-cols-2 sm:p-6"><Field label="Tipo da cirurgia *" wide><input required className="field" value={form.title??''} onChange={e=>set('title',e.target.value)} placeholder="Ex.: Colecistectomia, cesariana..."/></Field><Field label="Hospital / contratante *" wide><select required className="field" value={form.contractor_id??''} onChange={e=>{const c=contractors?.items.find(x=>x.id===e.target.value);setForm({...form,contractor_id:e.target.value,hospital_sector:c?.name??form.hospital_sector})}}><option value="">Selecione</option>{contractors?.items.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></Field><Field label="Hospital / setor"><input className="field" value={form.hospital_sector??''} onChange={e=>set('hospital_sector',e.target.value)}/></Field><Field label="Especialidade"><input className="field" value={form.specialty??''} onChange={e=>set('specialty',e.target.value)}/></Field><Field label="Data *"><input required type="date" className="field" value={form.date??''} onChange={e=>set('date',e.target.value)}/></Field><Field label="Horário inicial *"><input required type="time" className="field" value={form.start_time??''} onChange={e=>set('start_time',e.target.value)}/></Field><Field label="Horário final *"><input required type="time" className="field" value={form.end_time??''} onChange={e=>set('end_time',e.target.value)}/></Field><Field label="Honorários previstos"><input required min="1" type="number" className="field" value={form.gross_value??1} onChange={e=>set('gross_value',Number(e.target.value))}/></Field><Field label="Observações" wide><textarea className="field min-h-28" value={form.notes??''} onChange={e=>set('notes',e.target.value)} placeholder="Equipe, preparo, materiais ou outras informações..."/></Field></section>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}</form>}
function Field({label,wide,children}:{label:string;wide?:boolean;children:React.ReactNode}){return <label className={wide?'label sm:col-span-2':'label'}>{label}{children}</label>}
